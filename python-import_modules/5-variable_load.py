#!/usr/bin/python3

if __name__ == "__main__":
    # Importer uniquement la variable `a` depuis `variable_load_5`
    from variable_load_5 import a

    # Afficher la valeur de `a`
    print("{:d}".format(a))
